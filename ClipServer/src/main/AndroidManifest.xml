<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cs478.clipserver">
    <permission android:name="com.cs478.clipserver.ACCESS_SERVICE"
        android:protectionLevel="signature"/>
    <!-- Permissions for foreground service and media playback -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"    />
    <application
        android:allowBackup="true"
        android:exported="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClipServer">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Service declaration -->
        <service
            android:name=".AudioService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            android:permission="com.cs478.clipserver.ACCESS_SERVICE">
            <intent-filter>
                <action android:name="com.cs478.clipserver.ACTION_START_AUDIO_SERVICE"/>
            </intent-filter>
        </service>
    </application>
</manifest>
